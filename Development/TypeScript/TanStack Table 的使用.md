## 无头样式库（Headless UI Library）是什么

“无头”指的是**只提供逻辑与行为，不提供任何预设的 UI 外观（CSS）和固定的 DOM 结构**。通俗来说，它给你一套“大脑”和“骨架”，但“长相”完全由你决定。

目前比较流行的组件库如： tailwindcss shadcn 等，与传统的（ant design。。。）不同，把组件的行为与样式进行分离，也更适合与 ai 协同编程。